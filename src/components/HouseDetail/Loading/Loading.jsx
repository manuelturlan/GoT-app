import React from 'react';
import ReactLoading from 'react-loading'

const Loading = () => {
    return (
        <ReactLoading type={'spin'} color={'black'} height={'20%'} width={'20%'}></ReactLoading>
    );
}

export default Loading;
